#GORON Nathan 21503237
"""import socket


def affiche():
    res=""
    for i in g:
        for j in i:
            res+=str(j)+" "
        res+="\n"
    print (res)

host="local"
port=15555

socket = socket.socket(socket.AF_INET, socket.SOCK_STREAM)
socket.connect((host, port))

def demande():
    socket.send(exe)
    
    







socket.close()"""
